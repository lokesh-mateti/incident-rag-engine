apiVersion: v1
kind: Service
metadata:
  name: {{ include "incident-rag.fullname" . }}
  labels:
    {{- include "incident-rag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "incident-rag.selectorLabels" . | nindent 4 }}
