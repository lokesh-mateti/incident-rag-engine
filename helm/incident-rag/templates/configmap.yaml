apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "incident-rag.fullname" . }}-config
  labels:
    {{- include "incident-rag.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
